---
ansible-navigator:
  ansible:
    config:
      path: ansible.cfg
  execution-environment:
    image: hub.rh.lab/network-ee:latest
    # image: quay.io/maxrainer/netbox:latest
    pull:
      policy: missing
  playbook-artifact:
    enable: false
